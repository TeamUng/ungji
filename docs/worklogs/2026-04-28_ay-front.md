# Worklog — ay-front

## 작업 범위

- 프론트 전용 `TODO_FRONTEND.md`에 권장 진행 흐름을 추가했다.
- `frontend/` 하위에 React + Vite + TypeScript + Tailwind CSS 앱을 초기 세팅했다.
- Vercel Preview 배포를 위한 `frontend/vercel.json`, `.env.example`, 실행 문서를 추가했다.
- 배포 확인용으로 `오늘의 학습` 화면과 AI 코치 플로팅 말풍선의 최소 UI를 구현했다.
- 참고 이미지의 둥근 비율과 밝은 표정만 반영한 오리지널 주황/진한 노랑 캐릭터 SVG를 추가했다.
- 캐릭터를 큰 귀, 크림색 얼굴, `><` 눈, 분홍 웃는 입 중심의 단순한 얼굴형으로 재조정했다.
- 귀 사이 중앙 골, 더 넓은 눈 간격, 입술이 아닌 입 벌린 웃음 형태로 캐릭터 비율을 다시 조정했다.
- 참고 이미지처럼 큰 양쪽 귀와 낮은 중앙 머리선이 보이도록 외곽을 다시 다듬고, 둥둥 뜨는 CSS 애니메이션은 유지했다.
- 입 모양을 빨간색/흰색 디테일 없이 연분홍색 단일 형태로 단순화했다.
- FE2~FE7 mock-first 프론트 흐름을 구현했다.
- 케이스 1/2 학생, 오늘의 학습 태스크, 국어/수학 문제, TP1~TP5 mock 응답 데이터를 추가했다.
- 오늘의 학습 화면, 학습 화면, AI 코치 플로팅 말풍선, 오른쪽 채팅창, 메시지 타입 렌더러를 컴포넌트로 분리했다.
- 실제 API 연결 전까지 사용할 `mockChatClient`와 `useChatSession`을 추가했다.
- 학습 화면의 `나가려 해요/학습 완료/오늘 끝내기` 버튼은 실제 학생용 UI가 아니라 TP3/TP2/TP5 시연 트리거임을 확인하고, `시연:` 컨트롤로 문구와 위치를 조정했다.
- 케이스 선택 UI를 본문 위 플로팅에서 상단바로 옮겨 학습 헤더와 겹치지 않게 정리했다.

## 검증

```bash
cd frontend
npm install
npm run typecheck
npm run build
npm run dev -- --host 127.0.0.1
xmllint --noout frontend/public/assets/coach-avatar.svg
```

- `npm install`: 통과, 취약점 0개
- `npm run typecheck`: 통과
- `npm run build`: 통과
- `npm run dev -- --host 127.0.0.1`: `http://127.0.0.1:5173/` 응답 확인
- `coach-avatar.svg`: XML 문법 검증 통과, dev 서버에서 `image/svg+xml`로 응답 확인
- `story-scene.svg`, `ratio-visual.svg`: dev 서버에서 `image/svg+xml` 응답 확인

## 다음 작업

- Vercel에서 Root Directory를 `frontend`로 지정하고 `ay-front` 브랜치 Preview 배포를 확인한다.
- 케이스 1/2 전체 흐름을 브라우저에서 수동 클릭 검수한다.
- FE8 실제 API 연결 전 FE9 프론트-백엔드 연결 합의 항목을 확정한다.
